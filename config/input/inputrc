# Switch to Vi mode.
set editing-mode vi

# Clear the screen but keep the current line.
C-L: clear-screen

# Directly insert the last argument of the previous command.
C-O: insert-last-argument

# When going up or down in the command history, match on what's been typed so
# far.
"\e[A": history-search-backward
"\e[B": history-search-forward
