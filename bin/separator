#!/bin/bash -e

main() {
    local color="${2:-4}"
    local cols="$(tput cols)"
    local separator_char="${1:-=}"
    tput setaf "$color"
    for _ in $(seq 1 "$cols"); do
        echo -n "$separator_char"
    done
    echo
    tput sgr0
}

main "$@"
