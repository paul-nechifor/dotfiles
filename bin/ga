#!/bin/bash

main() {
  if svn info &>/dev/null; then
    svn-color add "$@"
  else
    if [[ $# -eq 0 ]]; then
      git add --all .
    else
      git add --all "$@"
    fi
  fi
  gs .
}

main "$@"
