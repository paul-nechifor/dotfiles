#!/usr/bin/env python

import os
import sys

project_file = sys.argv[1].replace('/', '-')
commands_dir = os.getenv('HOME') + '/.local-build-commands'

for file in os.listdir(commands_dir):
  if project_file.startswith(file):
    os.system('bash ' + commands_dir + '/' + file)
