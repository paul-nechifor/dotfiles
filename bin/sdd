#!/usr/bin/env bash

collector_file="$HOME/svn-vimdiff-arguments"
cmd="$HOME/.pn-dotfiles/bin/svn-diff-collector"

echo -n "vim +'" > "$collector_file"
svn diff --diff-cmd "$cmd" "$@" >/dev/null
echo -n " tabfirst | q'" >> "$collector_file"

bash "$collector_file"
rm "$collector_file"
