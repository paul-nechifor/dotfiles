#!/bin/bash

if [ $# -eq 1 ]; then
  amixer set Master ${1}0%
  exit
fi

regex="\[([0-9]+)%\]"
[[ "`amixer get Master`" =~ $regex ]]
volume=${BASH_REMATCH[1]}

if [ $volume -eq 100 ]; then
  amixer set Master 0%
else
  amixer set Master 100%
fi
