#!/bin/bash

main() {
  if svn info &>/dev/null; then
    svn co "$@"
  else
    git clone "$@"
  fi

  separator —
  if [[ "$2" ]]; then
    d "$2"
  else
    d "$(basename "$1" | sed 's/\.git$//')"
  fi
}

main "$@"
